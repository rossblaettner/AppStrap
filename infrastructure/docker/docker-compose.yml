
version: '3.9'

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: appstrap
      POSTGRES_PASSWORD: appstrap
      POSTGRES_DB: appstrap
    ports:
      - "5432:5432"
